#include <iostream>
#include <vector>

using std::vector;
using std::cout;
using std::endl;
using std::cerr;
using std::runtime_error;
using Matrix = std::vector<vector<int>>;

Matrix naive(Matrix& matrix1, Matrix& matrix2);
